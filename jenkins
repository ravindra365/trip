pipeline {
	// Keeps only 5 build
	options {
        buildDiscarder(logRotator(numToKeepStr: '5', artifactNumToKeepStr: '5'))
    }
    agent any
    
	stages{
		stage('Print maven version'){
			steps{
				sh 'mvn --version'
				}
									}		
	 
		}
}